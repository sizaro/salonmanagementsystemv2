import { Quote, Star } from "lucide-react";
import { motion } from "framer-motion";
import SectionHeading from "../public/SectionHeading.jsx";
import { testimonials } from "./homeData.js";

export default function Testimonials() { return <section className="salon-section bg-white"><div className="salon-container"><SectionHeading eyebrow="Client confidence" title="The feeling guests take with them." description="Professional results matter. So does how you are treated along the way." align="center" /><div className="mt-12 grid gap-6 lg:grid-cols-3">{testimonials.map((item, index) => <motion.blockquote key={item.name} initial={{ opacity: 0, y: 30 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: index * .1 }} whileHover={{ y: -6 }} className="rounded-[2rem] border border-stone-200 bg-[var(--salon-cream)] p-7"><div className="flex items-center justify-between"><Quote className="text-[var(--salon-copper)]" /><span className="flex gap-1 text-amber-500">{Array.from({ length: 5 }, (_, star) => <Star key={star} size={14} fill="currentColor" />)}</span></div><p className="mt-6 font-serif text-xl leading-8 text-slate-800">“{item.quote}”</p><footer className="mt-7 border-t border-stone-200 pt-5"><p className="font-semibold">{item.name}</p><p className="text-sm text-slate-500">{item.service}</p></footer></motion.blockquote>)}</div></div></section>; }
